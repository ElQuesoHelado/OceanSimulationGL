use super::*;

struct Wave {
    amplitude: f32,
    frequency: f32,
    direction: f32,
    phase: f32,
}

pub struct Ocean {
    time: f32,
    npoints: usize,
    instance_id: usize,
}

impl Ocean {
    // pub fn new() -> Self {
    //     Self {}
    // }
}
